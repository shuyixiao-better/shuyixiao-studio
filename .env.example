# ==========================================
# 用户反馈邮件配置示例
# ==========================================
# 
# 使用说明：
# 1. 复制此文件为 .env
# 2. 填入真实的配置信息
# 3. .env 文件不会被提交到 Git（已在 .gitignore 中）
#
# ⚠️ 重要：授权密码不是登录密码！
#    需要在 163 邮箱设置中单独获取
# ==========================================

# SMTP 服务器地址（163 邮箱）
SMTP_HOST=smtp.163.com

# SMTP 端口（465 使用 SSL 加密，推荐）
SMTP_PORT=465

# 发件邮箱（你的 163 邮箱地址）
SMTP_USER=your-email@163.com

# SMTP 授权密码（在 163 邮箱设置中获取，不是登录密码！）
# 获取方式：登录 163 邮箱 → 设置 → POP3/SMTP/IMAP → 开启服务 → 获取授权密码
SMTP_PASS=your-authorization-password-here

# 接收反馈的邮箱（可以与发件邮箱相同）
FEEDBACK_RECEIVER=your-email@163.com

# ==========================================
# 配置完成后：
# 1. 本地开发：直接运行 pnpm docs:dev
# 2. Netlify 部署：在后台配置相同的环境变量
# 3. 测试：访问网站点击右下角反馈按钮
# ==========================================

# ==========================================
# PandaCoder 周报服务配置
# ==========================================
# PandaCoder 前端服务地址（React 应用）
# 本地开发: http://localhost:5174
# 生产环境: http://your-server-ip:5174 或 https://your-domain.com
PANDACODER_FRONTEND_URL=http://localhost:5174

# PandaCoder 后端服务地址（Spring Boot API）
# 本地开发: http://localhost:8080
# 生产环境: http://your-server-ip:8080 或 https://api.your-domain.com
PANDACODER_BACKEND_URL=http://localhost:8080

# ==========================================
# 配置说明：
# 1. 将 localhost 替换为实际的服务器 IP 或域名
# 2. 确保端口号与实际运行的服务一致
# 3. Netlify 部署时在后台配置这两个环境变量
# 4. GitHub Actions 部署时在 Secrets 中配置
# ==========================================

# ==========================================
# 评论功能配置
# ==========================================
# 管理员邮箱（接收评论通知）
ADMIN_EMAIL=your-email@163.com

# 管理员密码（用于删除评论）
# 建议使用强密码，至少8位，包含字母和数字
ADMIN_PASSWORD=your-strong-password-here

# ==========================================
# 配置说明：
# 1. ADMIN_EMAIL: 接收评论通知的邮箱地址
# 2. ADMIN_PASSWORD: 删除评论时需要输入的密码
# 3. 评论数据存储在 Netlify Blobs 中
# 4. GitHub Pages 部署时评论功能不可用（仅 Netlify 支持）
# ==========================================
